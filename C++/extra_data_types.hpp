struct TreeNode {
    int val;
    TreeNode* left;
    TreeNode* right;
    TreeNode(int x) : val(x), left(nullptr), right(nullptr) {}
};

struct MyListNode {
    int val;
    MyListNode* next;
    MyListNode(int x) : val(x), next(nullptr) {}
};
